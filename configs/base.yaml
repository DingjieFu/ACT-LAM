defaults:
  - /paths
  - override hydra/job_logging: none
  - _self_

seed: 42

representation:
  mode: feature

reproducibility:
  deterministic: true
  warn_only: true

wandb:
  enabled: true
  project: ACTLAM
  run_name: null
