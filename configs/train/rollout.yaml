defaults:
  - /train/common
  - _self_

task: rollout_finetune

training:
  stage: rollout_finetune
  selection_metric: rollout_state_mse
  max_steps: 1000
  val_every_steps: 200
  checkpoint_every_steps: 200
  periodic_checkpoint_every_steps: 1000
